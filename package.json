{
  "name": "argus",
  "version": "0.1.0",
  "description": "ARGUS - ATLAS Resource Graphical User System",
  "type": "module",
  "scripts": {
    "version:generate": "bash scripts/generate-version.sh",
    "build": "npm run version:generate && tsc",
    "watch": "npm run version:generate && tsc --watch",
    "serve": "npx http-server dist -p 8080 -c-1",
    "dev": "npm run build && npm run serve"
  },
  "keywords": [
    "atlas",
    "argus",
    "medical-imaging",
    "federated-learning",
    "lcars"
  ],
  "author": "ATLAS Project Team",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0",
    "http-server": "^14.1.1"
  }
}
